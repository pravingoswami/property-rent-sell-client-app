const initialFeaturesState = []

const FeaturesReducers = (state = initialFeaturesState, action) => {
    switch(action.type){
        default : {
            return state
        }
    }
}

export default FeaturesReducers