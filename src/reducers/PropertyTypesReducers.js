const initialPropertyTypesState = []

const PropertyTypesReducers = (state = initialPropertyTypesState, action) => {
    switch(action.type){
        default : {
            return state
        }
    }
}

export default PropertyTypesReducers